plugin for SynWrite.
gives dialog to insert numbers from start-number, with increment 1, with prefix/suffix strings.
if selection exists, then selected lines will be numbered at line-start.
if no selection, then repeat counter is used in dialog, numbers inserted at start of caret's line.
only one caret allowed.

author: Alexey T.
